logging:
  level:
    org.springframework: ERROR
    logging.level.yuna.berlin: INFO

nats:
  server:
    hb_fail_count: 3
  source:
    default: file://${user.dir}/src/test/resources/natsserver/mac.zip